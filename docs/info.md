Add daily calendar, for daily tasks alongside, notes, todos. and a UI changes in todo page, for showing total todo pending and completed on the right side, show the todos that are pending on the left side. The calendar UI goes like this, has folders(let's say self-improvement), on the folder adding page the bottom-action-button is used to add folders with title, description. Then upon visiting a folder, bottom-action-button will add the tasks(tasks title description),  lists tasks like this <><>. store the days on which the tasks were completed, mark the uncompleted days with red, and completed with green, on the left side list the tasks title as column, then next to it a table with showing each square showing whether the task for that day is completed or not.




Add daily calendar, for daily tasks alongside, notes, todos. and a UI changes in todo page, for showing total todo pending and completed on the right
  side, show the todos that are pending on the left side. The calendar UI goes like this, has folders(let's say self-improvement), on the folder adding
  page the bottom-action-button is used to add folders with title, description. Then upon visiting a folder, bottom-action-button will add the tasks(tasks
  title description),  lists tasks like this <><>. store the days on which the tasks were completed, mark the uncompleted days with red, and completed with
  green, on the left side list the tasks title as column, then next to it a table with showing each square showing whether the task for that day is
  completed or not. use @docs/wise-design.md  for design tasks.png shows you how to create the tasks UI  @photos/tasks.png, also cache everything locally
  and check cache first before going to database okay, then also keep the state persistant like other parts of UI.
